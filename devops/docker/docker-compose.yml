version: '3.8'

services:
  # Simple React app for academic demo
  scrunchcreate-web:
    build:
      context: ../..
      dockerfile: devops/docker/Dockerfile
    container_name: scrunchcreate-web
    ports:
      - "4173:4173"
    restart: unless-stopped
